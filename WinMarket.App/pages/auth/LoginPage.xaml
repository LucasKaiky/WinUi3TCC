<Page
    x:Class="WinMarket.App.pages.auth.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Width="360" HorizontalAlignment="Center" Spacing="12">
            <TextBlock Text="Login" FontSize="28" HorizontalAlignment="Center" Margin="0,0,0,8"/>
            <TextBox PlaceholderText="Usuário" Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <PasswordBox x:Name="Pwd" PlaceholderText="Senha"/>
            
            <Button Content="Entrar" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=Pwd, Path=Password}" Width="360" Height="40"/>
            
            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <ProgressRing IsActive="{Binding IsBusy}" Width="20" Height="20"/>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="IndianRed" TextWrapping="WrapWholeWords"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
